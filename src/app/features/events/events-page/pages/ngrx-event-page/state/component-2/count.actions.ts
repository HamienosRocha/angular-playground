import { createAction } from "@ngrx/store";

export const component2Increment = createAction('[Component 2 Counter] increment');
export const component2Decrement = createAction('[Component 2 Counter] decrement');